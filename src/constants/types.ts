export interface sidebarTypes {
    id: number;
    name: string;
    icon: JSX.Element;
    link: string;
  }

export interface lineTypes {
    variant: string;
}
export interface cardTypes {
  name: string;
  number: number;
}

